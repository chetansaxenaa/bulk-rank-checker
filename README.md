# bulk-rank-checker
Bulk keyword rank checker with country + city-level geo-targeting. Paste keywords, get Google ranking positions for your domain — with smart auto-detection of locations inside each keyword, or a manual location search with live suggestions. Runs entirely in-browser using your own Serper.dev API key.
